import { component$ } from "@builder.io/qwik";

export default component$(() => {
  return (
    <header class="px-5 py-3 text-center">
      <h1 class="text-lg font-bold">Placeholder Image Generator</h1>
    </header>
  );
});
