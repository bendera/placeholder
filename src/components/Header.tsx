import { component$ } from "@builder.io/qwik";

export default component$(() => {
  return (
    <header class="sticky top-0 bg-blue-500 px-5 py-3 text-white shadow-md">
      <h1 class="text-lg font-bold">Placeholder Image Generator</h1>
    </header>
  );
});
